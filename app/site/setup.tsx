import { ReactNode, useState } from 'react';
import { Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import { AppScreen } from '../../components/AppScreen';
import { useSitePlanner } from '../../data/sitePlannerStore';
import { getEffectiveProgrammeWeeks, TemplateActivity } from '../../utils/templateProgramme';

const allowedStages = [1, 2, 4, 5, 6, 7, 8, 9] as const;
const workingDayChoices = [5, 6, 7] as const;
type AllowedStage = typeof allowedStages[number];
type WorkingDays = typeof workingDayChoices[number];
type BuildRoute = 'Traditional' | 'Timber Frame';

function cleanCode(value: string) { return value.trim().replace(/\s+/g, ' '); }
function toStage(value: string): AllowedStage { const parsed = Number(value); return allowedStages.includes(parsed as AllowedStage) ? parsed as AllowedStage : 6; }
function reorderActivities(activities: TemplateActivity[]) { return activities.slice().sort((a, b) => a.order - b.order).map((activity, index) => ({ ...activity, order: index + 1, overlapAllowed: activity.overlapAllowed ?? false })); }
function currentWorkingDays(setup: { includeSaturday?: boolean; includeSunday?: boolean }): WorkingDays { if (setup.includeSunday) return 7; if (setup.includeSaturday) return 6; return 5; }
function workingWeekLabel(days: WorkingDays) { if (days === 7) return '7 days - Monday to Sunday'; if (days === 6) return '6 days - Monday to Saturday'; return '5 days - Monday to Friday'; }
function workingWeekChanges(days: WorkingDays) { return { workingWeek: workingWeekLabel(days), includeSaturday: days >= 6, includeSunday: days >= 7 }; }

export default function SiteSetupScreen() {
  const { siteSetup, plotTemplates, sitePlots, resetPlotData, updateSiteSetup, updatePlotTemplate, updateTemplateActivityDuration } = useSitePlanner();
  const [selectedBuildRoute, setSelectedBuildRoute] = useState<BuildRoute>('Traditional');
  const [selectedTemplateId, setSelectedTemplateId] = useState(plotTemplates[2]?.id ?? plotTemplates[0]?.id ?? 'threeBed');
  const [saved, setSaved] = useState(false);
  const [message, setMessage] = useState('');
  const plotTypeTemplates = plotTemplates.filter((template) => template.id !== 'timberFrame');
  const selectedPlotTypeTemplate = plotTypeTemplates.find((template) => template.id === selectedTemplateId) ?? plotTypeTemplates[0] ?? plotTemplates[0];
  const timberFrameTemplate = plotTemplates.find((template) => template.id === 'timberFrame');
  const selectedTemplate = selectedBuildRoute === 'Timber Frame' && timberFrameTemplate ? timberFrameTemplate : selectedPlotTypeTemplate;
  const orderedActivities = selectedTemplate ? reorderActivities(selectedTemplate.activities) : [];
  const activeWorkingDays = currentWorkingDays(siteSetup);
  const markChanged = () => setSaved(false);
  const saveSiteSetup = (changes: Partial<typeof siteSetup>) => { markChanged(); updateSiteSetup(changes); };
  const savePlotTemplate = (template: typeof selectedTemplate) => { if (!template) return; markChanged(); updatePlotTemplate(template); };
  const updateActivity = (activityCode: string, changes: Partial<TemplateActivity>) => {
    if (!selectedTemplate) return;
    markChanged();
    const nextActivities = selectedTemplate.activities.map((activity) => activity.code !== activityCode ? activity : { ...activity, ...changes, code: changes.code ? cleanCode(String(changes.code)) : activity.code, overlapAllowed: activity.overlapAllowed ?? false });
    updatePlotTemplate({ ...selectedTemplate, activities: reorderActivities(nextActivities) });
  };
  const updateDuration = (templateId: string, activityCode: string, durationDays: number) => { markChanged(); updateTemplateActivityDuration(templateId, activityCode, durationDays); };
  const moveFix = (activity: TemplateActivity, direction: -1 | 1) => {
    if (!selectedTemplate) return;
    const currentActivities = reorderActivities(selectedTemplate.activities);
    const currentIndex = currentActivities.findIndex((item) => item.code === activity.code);
    const nextIndex = currentIndex + direction;
    if (currentIndex < 0 || nextIndex < 0 || nextIndex >= currentActivities.length) return;
    const nextActivities = currentActivities.slice();
    [nextActivities[currentIndex], nextActivities[nextIndex]] = [nextActivities[nextIndex], nextActivities[currentIndex]];
    markChanged();
    updatePlotTemplate({ ...selectedTemplate, activities: reorderActivities(nextActivities) });
    setMessage(`${activity.code} moved ${direction < 0 ? 'up' : 'down'}.`);
  };
  const addFixAfter = (activity: TemplateActivity) => {
    if (!selectedTemplate) return;
    const insertOrder = activity.order + 1;
    const uniqueSuffix = Date.now().toString().slice(-5);
    const newActivity: TemplateActivity = { order: insertOrder, code: `New fix ${uniqueSuffix}`, trade: activity.trade, displayText: 'New Fix', durationDays: 1, relativeWeek: 1, relativeDay: 1, stage: activity.stage, overlapAllowed: false };
    const nextActivities = selectedTemplate.activities.map((item) => (item.order >= insertOrder ? { ...item, order: item.order + 1 } : item)).concat(newActivity);
    markChanged(); updatePlotTemplate({ ...selectedTemplate, activities: reorderActivities(nextActivities) }); setMessage(`New fix added below ${activity.code}. Edit the row in the table.`);
  };
  const removeFix = (activity: TemplateActivity) => {
    if (!selectedTemplate) return;
    markChanged(); updatePlotTemplate({ ...selectedTemplate, activities: reorderActivities(selectedTemplate.activities.filter((item) => item.code !== activity.code)) }); setMessage(`${activity.code} removed.`);
  };

  return (
    <AppScreen>
      <View style={styles.header}><Text style={styles.eyebrow}>Site Setup</Text><Text style={styles.title}>Programme & Plot Templates</Text><Text style={styles.subtitle}>Edit the build route, working week and plot type sequence directly in the table.</Text></View>
      <View style={styles.card}><Text style={styles.cardTitle}>Site programme defaults</Text><View style={styles.formGrid}><Field label="Site name"><TextInput value={siteSetup.siteName} onChangeText={(siteName) => saveSiteSetup({ siteName })} placeholder="Site name" style={styles.input} /></Field><Field label="Default programme weeks"><TextInput value={String(siteSetup.defaultProgrammeWeeks)} onChangeText={(value) => saveSiteSetup({ defaultProgrammeWeeks: Number(value) || 0 })} keyboardType="number-pad" style={styles.input} /></Field><Field label="Default stage count"><TextInput value={String(siteSetup.stageCount)} onChangeText={(value) => saveSiteSetup({ stageCount: Number(value) || 0 })} keyboardType="number-pad" style={styles.input} /></Field><Field label="Working week"><View style={styles.optionRow}>{workingDayChoices.map((days) => { const active = days === activeWorkingDays; return <Pressable key={days} style={[styles.smallChip, active ? styles.smallChipActive : null]} onPress={() => saveSiteSetup(workingWeekChanges(days))}><Text style={[styles.smallChipText, active ? styles.smallChipTextActive : null]}>{days} days</Text></Pressable>; })}</View><Text style={styles.helpText}>{workingWeekLabel(activeWorkingDays)}</Text></Field></View></View>
      <View style={styles.warningCard}><View style={styles.warningTextWrap}><Text style={styles.warningTitle}>Plot data reset</Text><Text style={styles.warningText}>Clears all current plots and plot delays so you can add your own site data and test from a clean start.</Text><Text style={styles.warningMeta}>Current plots: {sitePlots.length}</Text></View><Pressable style={styles.dangerButton} onPress={resetPlotData}><Text style={styles.dangerButtonText}>Reset plot data</Text></Pressable></View>
      <View style={styles.card}>
        <View style={styles.cardHeadingRow}><View><Text style={styles.cardTitle}>Build route and plot type templates</Text><Text style={styles.helpText}>Select Traditional or Timber Frame, then choose the bedroom size underneath.</Text></View>{message ? <Text style={styles.message}>{message}</Text> : null}</View>
        <View style={styles.routeChips}>{(['Traditional', 'Timber Frame'] as BuildRoute[]).map((route) => { const active = route === selectedBuildRoute; return <Pressable key={route} style={[styles.routeChip, active ? styles.routeChipActive : null]} onPress={() => { setSelectedBuildRoute(route); setMessage(`${route} route selected.`); }}><Text style={[styles.routeChipText, active ? styles.routeChipTextActive : null]}>{route}</Text></Pressable>; })}</View>
        <ScrollView horizontal showsHorizontalScrollIndicator={false}><View style={styles.templateChips}>{plotTypeTemplates.map((template) => { const active = template.id === selectedTemplateId; return <Pressable key={template.id} style={[styles.templateChip, active ? styles.templateChipActive : null]} onPress={() => { setSelectedTemplateId(template.id); setMessage(''); }}><Text style={[styles.templateChipText, active ? styles.templateChipTextActive : null]}>{template.name}</Text></Pressable>; })}</View></ScrollView>
        {selectedTemplate ? <View style={styles.templatePanel}><View style={styles.formGrid}><Field label="Template name"><TextInput value={selectedTemplate.name} onChangeText={(name) => savePlotTemplate({ ...selectedTemplate, name })} style={styles.input} /></Field><Field label="Build route"><TextInput value={selectedBuildRoute} editable={false} style={styles.input} /></Field><Field label="Target weeks"><TextInput value={String(selectedTemplate.programmeWeeks)} onChangeText={(value) => savePlotTemplate({ ...selectedTemplate, programmeWeeks: Number(value) || 0 })} keyboardType="number-pad" style={styles.input} /></Field><Field label="Stage count"><TextInput value={String(selectedTemplate.stageCount)} onChangeText={(value) => savePlotTemplate({ ...selectedTemplate, stageCount: Number(value) || 0 })} keyboardType="number-pad" style={styles.input} /></Field><View style={styles.summaryBox}><Text style={styles.summaryLabel}>Calculated weeks</Text><Text style={styles.summaryValue}>{getEffectiveProgrammeWeeks(selectedTemplate, siteSetup)}</Text></View></View>
          <ScrollView horizontal showsHorizontalScrollIndicator><View><View style={styles.tableRow}><Text style={[styles.headerCell, styles.addCell]}>Add</Text><Text style={[styles.headerCell, styles.moveCell]}>Move</Text><Text style={[styles.headerCell, styles.orderCell]}>Seq</Text><Text style={[styles.headerCell, styles.taskCell]}>Task</Text><Text style={[styles.headerCell, styles.tradeCell]}>Trade</Text><Text style={[styles.headerCell, styles.displayCell]}>Display</Text><Text style={[styles.headerCell, styles.stageCell]}>Stage</Text><Text style={[styles.headerCell, styles.durationCell]}>Days</Text><Text style={[styles.headerCell, styles.removeCell]}>Remove</Text></View>{orderedActivities.map((activity, index) => <View key={`${activity.order}-${activity.code}`} style={[styles.tableRow, index % 2 ? styles.altRow : null]}><Pressable style={styles.addButton} onPress={() => addFixAfter(activity)}><Text style={styles.addButtonText}>+</Text></Pressable><View style={styles.moveButtons}><Pressable style={styles.moveButton} onPress={() => moveFix(activity, -1)}><Text style={styles.moveButtonText}>↑</Text></Pressable><Pressable style={styles.moveButton} onPress={() => moveFix(activity, 1)}><Text style={styles.moveButtonText}>↓</Text></Pressable></View><Text style={[styles.bodyCell, styles.orderCell]}>{activity.order}</Text><TextInput defaultValue={activity.code} onEndEditing={(event) => updateActivity(activity.code, { code: event.nativeEvent.text })} style={[styles.bodyInput, styles.taskCell]} /><TextInput defaultValue={activity.trade} onEndEditing={(event) => updateActivity(activity.code, { trade: event.nativeEvent.text })} style={[styles.bodyInput, styles.tradeCell]} /><TextInput defaultValue={activity.displayText} onEndEditing={(event) => updateActivity(activity.code, { displayText: event.nativeEvent.text })} style={[styles.bodyInput, styles.displayCell]} /><TextInput defaultValue={String(activity.stage)} keyboardType="number-pad" onEndEditing={(event) => updateActivity(activity.code, { stage: toStage(event.nativeEvent.text) })} style={[styles.bodyInput, styles.stageCell]} /><TextInput defaultValue={String(activity.durationDays)} keyboardType="number-pad" onEndEditing={(event) => updateDuration(selectedTemplate.id, activity.code, Number(event.nativeEvent.text) || 0)} style={[styles.durationInput, styles.durationCell]} /><Pressable style={styles.removeButton} onPress={() => removeFix(activity)}><Text style={styles.removeButtonText}>-</Text></Pressable></View>)}</View></ScrollView></View> : null}
        <Pressable style={[styles.primaryButton, saved ? styles.savedButton : null]} onPress={() => setSaved(true)}><Text style={styles.primaryButtonText}>{saved ? 'Saved' : 'Save'}</Text></Pressable>
      </View>
    </AppScreen>
  );
}

function Field({ label, children }: { label: string; children: ReactNode }) { return <View style={styles.field}><Text style={styles.label}>{label}</Text>{children}</View>; }

const styles = StyleSheet.create({
  header: { gap: 4 }, eyebrow: { color: '#2563eb', fontSize: 12, fontWeight: '900', textTransform: 'uppercase' }, title: { color: '#0f172a', fontSize: 30, fontWeight: '900' }, subtitle: { color: '#64748b', fontSize: 14, lineHeight: 20 }, card: { backgroundColor: '#ffffff', borderRadius: 18, borderWidth: 1, borderColor: '#e2e8f0', padding: 18, gap: 16 }, cardHeadingRow: { flexDirection: 'row', justifyContent: 'space-between', gap: 10, flexWrap: 'wrap', alignItems: 'flex-start' }, warningCard: { backgroundColor: '#fff7ed', borderRadius: 18, borderWidth: 1, borderColor: '#fed7aa', padding: 18, gap: 14, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', flexWrap: 'wrap' }, warningTextWrap: { flex: 1, minWidth: 240 }, warningTitle: { color: '#9a3412', fontSize: 18, fontWeight: '900' }, warningText: { color: '#9a3412', fontSize: 13, lineHeight: 19, marginTop: 5 }, warningMeta: { color: '#7c2d12', fontSize: 12, fontWeight: '900', marginTop: 8 }, dangerButton: { backgroundColor: '#c2410c', borderRadius: 12, paddingHorizontal: 14, paddingVertical: 11 }, dangerButtonText: { color: '#ffffff', fontWeight: '900', fontSize: 13 }, cardTitle: { color: '#0f172a', fontSize: 18, fontWeight: '900' }, formGrid: { flexDirection: 'row', flexWrap: 'wrap', gap: 12, alignItems: 'flex-end' }, field: { gap: 8, minWidth: 150, flex: 1 }, label: { color: '#475569', fontSize: 13, fontWeight: '900' }, input: { borderWidth: 1, borderColor: '#cbd5e1', borderRadius: 12, paddingHorizontal: 12, paddingVertical: 10, color: '#0f172a', backgroundColor: '#ffffff', fontWeight: '800' }, optionRow: { flexDirection: 'row', flexWrap: 'wrap', gap: 8 }, smallChip: { borderWidth: 1, borderColor: '#cbd5e1', borderRadius: 12, paddingHorizontal: 12, paddingVertical: 9, backgroundColor: '#ffffff' }, smallChipActive: { backgroundColor: '#173b5f', borderColor: '#173b5f' }, smallChipText: { color: '#475569', fontSize: 12, fontWeight: '900' }, smallChipTextActive: { color: '#ffffff' }, helpText: { color: '#64748b', fontSize: 12, lineHeight: 18 }, message: { color: '#166534', fontWeight: '900', fontSize: 12, backgroundColor: '#dcfce7', borderColor: '#86efac', borderWidth: 1, borderRadius: 999, paddingHorizontal: 12, paddingVertical: 8, maxWidth: 420 }, summaryBox: { minWidth: 150, backgroundColor: '#eff6ff', borderRadius: 12, padding: 12 }, summaryLabel: { color: '#2563eb', fontSize: 12, fontWeight: '900' }, summaryValue: { color: '#0f172a', fontSize: 24, fontWeight: '900' }, routeChips: { flexDirection: 'row', gap: 10, flexWrap: 'wrap' }, routeChip: { borderWidth: 1, borderColor: '#cbd5e1', borderRadius: 14, paddingHorizontal: 16, paddingVertical: 11, backgroundColor: '#ffffff' }, routeChipActive: { backgroundColor: '#173b5f', borderColor: '#173b5f' }, routeChipText: { color: '#475569', fontSize: 13, fontWeight: '900' }, routeChipTextActive: { color: '#ffffff' }, templateChips: { flexDirection: 'row', gap: 8, paddingVertical: 2 }, templateChip: { borderWidth: 1, borderColor: '#cbd5e1', borderRadius: 999, paddingHorizontal: 12, paddingVertical: 8, backgroundColor: '#ffffff' }, templateChipActive: { backgroundColor: '#0f172a', borderColor: '#0f172a' }, templateChipText: { color: '#64748b', fontSize: 12, fontWeight: '900' }, templateChipTextActive: { color: '#ffffff' }, templatePanel: { gap: 14 }, tableRow: { flexDirection: 'row', alignItems: 'stretch' }, altRow: { backgroundColor: '#f8fafc' }, headerCell: { backgroundColor: '#173b5f', color: '#ffffff', fontWeight: '900', fontSize: 12, padding: 8, borderWidth: 1, borderColor: '#9fb6ce', textAlign: 'center' }, bodyCell: { color: '#0f172a', padding: 8, borderWidth: 1, borderColor: '#c8d7e6', fontWeight: '800' }, bodyInput: { color: '#0f172a', padding: 8, borderWidth: 1, borderColor: '#c8d7e6', backgroundColor: '#ffffff', fontWeight: '800' }, addCell: { width: 52, textAlign: 'center' }, moveCell: { width: 70, textAlign: 'center' }, orderCell: { width: 54, textAlign: 'center' }, taskCell: { width: 170 }, tradeCell: { width: 150 }, displayCell: { width: 140 }, stageCell: { width: 70, textAlign: 'center' }, durationCell: { width: 80, textAlign: 'center' }, durationInput: { color: '#0f172a', padding: 8, borderWidth: 1, borderColor: '#c8d7e6', backgroundColor: '#fff4cc', fontWeight: '900' }, removeCell: { width: 52, textAlign: 'center' }, addButton: { width: 52, borderWidth: 1, borderColor: '#c8d7e6', alignItems: 'center', justifyContent: 'center', backgroundColor: '#dcfce7' }, addButtonText: { color: '#166534', fontWeight: '900', fontSize: 18 }, moveButtons: { width: 70, flexDirection: 'row', borderWidth: 1, borderColor: '#c8d7e6', backgroundColor: '#eff6ff', alignItems: 'center', justifyContent: 'center', gap: 4 }, moveButton: { width: 28, height: 28, borderRadius: 8, alignItems: 'center', justifyContent: 'center', backgroundColor: '#ffffff', borderWidth: 1, borderColor: '#bfdbfe' }, moveButtonText: { color: '#1d4ed8', fontWeight: '900', fontSize: 16 }, removeButton: { width: 52, borderWidth: 1, borderColor: '#c8d7e6', alignItems: 'center', justifyContent: 'center', backgroundColor: '#fee2e2' }, removeButtonText: { color: '#991b1b', fontWeight: '900', fontSize: 18 }, primaryButton: { alignSelf: 'flex-start', backgroundColor: '#0f172a', borderRadius: 12, paddingHorizontal: 16, paddingVertical: 12 }, savedButton: { backgroundColor: '#16a34a' }, primaryButtonText: { color: '#ffffff', fontWeight: '900' },
});